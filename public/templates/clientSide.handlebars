      {{#each this}} 
      <div class="col-lg-3 col-sm-4 col-xs-6">
        <a href="{{link}}" target="_blank" class="thumbnail">
         <img src="{{images.standard_resolution.url}}" class="img-responsive">
       </a>
     </div>
      {{/each}}
